Component Spec Template

1. Purpose
- What problem does this component solve? When to use vs. avoid.

2. Anatomy
- Slots/parts (e.g., root, trigger, content, icon)

3. Variants & Sizes
- Variants (e.g., primary/secondary/ghost)
- Sizes (e.g., sm/md/lg)

4. States
- default, hover, focus-visible, disabled, loading, error

5. Accessibility
- Roles, names, keyboard interactions, focus management, ARIA

6. Tokens
- Colors, spacing, radius, shadows, typography applied (semantic, not raw values)

7. API
- Props, events, controlled/uncontrolled, composition guidelines

8. Examples
- Copy/paste snippets for common cases, do/don’t

9. Migration Notes
- Differences from app-specific implementations; deprecations

